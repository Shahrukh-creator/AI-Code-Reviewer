# Clean Code Guidelines

## Naming

Use descriptive names that communicate intent.

Avoid unclear variable names such as `x`, `tmp`, or `data` when a more specific name is possible.

## Functions

Prefer small functions with one clear responsibility.

Avoid deeply nested conditional logic.

## Duplication

Avoid duplicating the same logic in multiple places.

Extract reusable logic into functions or appropriate abstractions.